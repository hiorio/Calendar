import type { TimePickerLabPickerProps } from './time-picker-lab-picker.types';

/** iOS 전용 실험 컴포넌트의 타입·플랫폼 대체 파일. 실험 화면은 iOS에서만 노출한다. */
export function TimePickerLabPicker(_props: TimePickerLabPickerProps) {
  return null;
}
